; PlatformIO Project Configuration File
;
; TODO: CONFIGURE AFTER HARDWARE SELECTION — verify board name matches
; your specific ESP32 dev board (esp32dev is the generic default and
; works for most ESP32-WROOM-32 boards; change if yours differs).

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
